from linked_list_zip import __version__
from linked_list_zip import linked_list_zip 

def test_version():
    assert __version__ == '0.1.0'

def test_addToList(self, newData):
        newNode = Node(newData)
        self.head = newNode
        actual = newNode
        expected = newNode
        assert actual == expected   

def test_append(self):
        temp = self.head
        actual = temp.next
        expected = newNode
        assert actual == expected



def test_zip_lists(): 
    last = storeNode
    actual = storeNode
    expected = last
    assert actual == expected